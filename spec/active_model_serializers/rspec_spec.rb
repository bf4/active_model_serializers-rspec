require "spec_helper"

RSpec.describe ActiveModelSerializers::RSpec do
  # it { is_expected.to match_json_schema('movies/index') }
  # it { expect(controller).to use_serializer(Api::V1::NeedSerializer) }
  # # or the following, since it does not matter what you pass to `expect()`, since we won't use `actual` anyway:
  # it { is_expected.to use_serializer(Api::V1::NeedSerializer) }
end
